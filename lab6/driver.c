#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<time.h>
#include "quicksort.h"

void main(){
	/*FILE *fptr = fopen("data.txt","r");
	if(fptr == NULL){
		printf("this file isn't opening");
		return;
		}
	Element* ele_arr = readfromfile(fptr, 2000);
	//Stack a = createStack();
	//push_ele(a, 5, 6);
	//Stack_ele temp= pop_ele(a);
	//printf("%d %d\n",temp->lower, temp->higher);
	//quicksort(ele_arr, 0, 1999, 5);
	//for(int i =0; i<2000; i++)
	//	printf("%f \n", ele_arr[i]->empid);
	
	/*double *arr;
	arr = (double*) malloc(sizeof(double)*2);
	arr = testRun(ele_arr, 10);
	printf("quick sort time is %lf", arr[0]- arr[1]);
	int m;
	m = estimatecutoff(5,10);
	printf("%d", m);*/
	drawgraph(4, 1000);
	
}


